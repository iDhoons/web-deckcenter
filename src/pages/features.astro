---
import BaseLayout from '../layouts/BaseLayout.astro';
import Section from '../components/Section.astro';
import CTA from '../components/CTA.astro';

const features = [
  {
    title: "직관적인 3D 설계",
    description: "드래그 앤 드롭으로 누구나 쉽게 데크 레이아웃을 디자인할 수 있습니다. 복잡한 CAD 프로그램 없이 마우스 클릭만으로 프로페셔널한 설계가 가능합니다.",
    icon: "cube",
    details: [
      "실시간 3D 미리보기",
      "자유로운 크기 조절",
      "다양한 형태의 데크 지원",
      "계단 및 난간 추가"
    ]
  },
  {
    title: "실시간 자재 견적",
    description: "설계와 동시에 필요한 자재 수량과 예상 비용을 실시간으로 계산합니다. 예산에 맞는 최적의 설계를 찾아보세요.",
    icon: "calculator",
    details: [
      "자동 자재 수량 계산",
      "실시간 비용 업데이트",
      "다양한 자재별 가격 비교",
      "할인 및 대량 구매 가격"
    ]
  },
  {
    title: "풍부한 자재 라이브러리",
    description: "목재, 합성목(WPC), 알루미늄 등 100종 이상의 실제 자재를 시뮬레이션해 보세요. 색상, 질감, 패턴까지 미리 확인할 수 있습니다.",
    icon: "library",
    details: [
      "100+ 자재 종류",
      "실제 텍스처 적용",
      "색상 및 패턴 변경",
      "자재별 특성 정보"
    ]
  },
  {
    title: "전문 도면 내보내기",
    description: "완성된 설계를 PDF 도면, 자재 리스트, 견적서로 내보내기할 수 있습니다. 시공업체에 바로 전달 가능한 전문 문서를 생성합니다.",
    icon: "download",
    details: [
      "PDF 도면 생성",
      "상세 자재 리스트",
      "견적서 자동 생성",
      "고해상도 렌더링"
    ]
  },
  {
    title: "프로젝트 저장 및 공유",
    description: "설계한 프로젝트를 클라우드에 저장하고 언제든 수정할 수 있습니다. 링크로 다른 사람과 공유하거나 협업할 수 있습니다.",
    icon: "cloud",
    details: [
      "클라우드 자동 저장",
      "버전 히스토리",
      "링크 공유",
      "팀 협업 지원"
    ]
  },
  {
    title: "모바일 지원",
    description: "태블릿과 스마트폰에서도 설계를 확인하고 수정할 수 있습니다. 현장에서 고객에게 바로 보여줄 수 있습니다.",
    icon: "mobile",
    details: [
      "반응형 디자인",
      "터치 제스처 지원",
      "오프라인 모드",
      "현장 프레젠테이션"
    ]
  }
];
---

<BaseLayout title="기능 | 데크센터">
  <section class="hero-banner">
    <div class="hero-content">
      <span class="eyebrow">Features</span>
      <h1>강력한 기능, 쉬운 사용</h1>
      <p>복잡한 CAD 없이도 프로페셔널한 데크 설계가 가능합니다</p>
    </div>
  </section>

  <Section>
    <div class="features-list">
      {features.map((feature, index) => (
        <div class={`feature-row ${index % 2 === 1 ? 'reverse' : ''}`}>
          <div class="feature-content">
            <div class="feature-icon">
              {feature.icon === "cube" && (
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"></path>
                  <polyline points="3.27 6.96 12 12.01 20.73 6.96"></polyline>
                  <line x1="12" y1="22.08" x2="12" y2="12"></line>
                </svg>
              )}
              {feature.icon === "calculator" && (
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="4" y="2" width="16" height="20" rx="2"></rect>
                  <line x1="8" y1="6" x2="16" y2="6"></line>
                  <line x1="8" y1="10" x2="8" y2="10.01"></line>
                  <line x1="12" y1="10" x2="12" y2="10.01"></line>
                  <line x1="16" y1="10" x2="16" y2="10.01"></line>
                  <line x1="8" y1="14" x2="8" y2="14.01"></line>
                  <line x1="12" y1="14" x2="12" y2="14.01"></line>
                  <line x1="16" y1="14" x2="16" y2="14.01"></line>
                  <line x1="8" y1="18" x2="8" y2="18.01"></line>
                  <line x1="12" y1="18" x2="16" y2="18"></line>
                </svg>
              )}
              {feature.icon === "library" && (
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
                  <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path>
                </svg>
              )}
              {feature.icon === "download" && (
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                  <polyline points="7 10 12 15 17 10"></polyline>
                  <line x1="12" y1="15" x2="12" y2="3"></line>
                </svg>
              )}
              {feature.icon === "cloud" && (
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"></path>
                </svg>
              )}
              {feature.icon === "mobile" && (
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect>
                  <line x1="12" y1="18" x2="12.01" y2="18"></line>
                </svg>
              )}
            </div>
            <h2>{feature.title}</h2>
            <p>{feature.description}</p>
            <ul class="feature-details">
              {feature.details.map(detail => (
                <li>
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <polyline points="20 6 9 17 4 12"></polyline>
                  </svg>
                  {detail}
                </li>
              ))}
            </ul>
          </div>
          <div class="feature-visual">
            <div class="visual-placeholder">
              <span>기능 이미지</span>
            </div>
          </div>
        </div>
      ))}
    </div>
  </Section>

  <Section title="지금 바로 시작하세요" description="무료로 모든 기능을 체험해 보세요." variant="alt">
    <div class="cta-section">
      <CTA text="무료로 시작하기" link="/app" variant="primary" />
      <p class="cta-note">신용카드 불필요 · 언제든 업그레이드 가능</p>
    </div>
  </Section>
</BaseLayout>

<style>
  .hero-banner {
    background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-light) 100%);
    padding: 6rem 2rem;
    text-align: center;
    color: white;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .eyebrow {
    display: inline-block;
    font-size: 0.9rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    opacity: 0.9;
    margin-bottom: 1rem;
  }

  .hero-banner h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: white;
  }

  .hero-banner p {
    font-size: 1.25rem;
    opacity: 0.9;
  }

  .features-list {
    display: flex;
    flex-direction: column;
    gap: 6rem;
  }

  .feature-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .feature-row.reverse {
    direction: rtl;
  }

  .feature-row.reverse > * {
    direction: ltr;
  }

  .feature-content {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .feature-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 72px;
    height: 72px;
    background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-light) 100%);
    border-radius: 16px;
    color: white;
  }

  .feature-content h2 {
    font-size: 1.75rem;
    margin-top: 0.5rem;
  }

  .feature-content > p {
    color: var(--color-text-light);
    font-size: 1.1rem;
    line-height: 1.7;
  }

  .feature-details {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
    margin-top: 1rem;
  }

  .feature-details li {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem;
    color: var(--color-text-light);
  }

  .feature-details svg {
    color: var(--color-accent);
    flex-shrink: 0;
  }

  .feature-visual {
    aspect-ratio: 4 / 3;
    border-radius: 16px;
    overflow: hidden;
    background: var(--color-bg-alt);
  }

  .visual-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-text-light);
    font-size: 0.9rem;
    border: 2px dashed var(--color-gray-200);
    border-radius: 16px;
  }

  .cta-section {
    text-align: center;
    padding: 3rem;
    background: white;
    border-radius: 20px;
    box-shadow: 0 4px 24px rgba(0,0,0,0.06);
  }

  .cta-note {
    margin-top: 1rem;
    font-size: 0.9rem;
    color: var(--color-text-light);
  }

  :global([data-theme="dark"]) .cta-section {
    background: var(--color-bg-alt);
  }

  :global([data-theme="dark"]) .feature-visual {
    background: var(--color-bg);
  }

  :global([data-theme="dark"]) .visual-placeholder {
    border-color: rgba(255,255,255,0.1);
  }

  @media (max-width: 900px) {
    .hero-banner h1 {
      font-size: 2.25rem;
    }

    .feature-row {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .feature-row.reverse {
      direction: ltr;
    }

    .feature-details {
      grid-template-columns: 1fr;
    }

    .features-list {
      gap: 4rem;
    }
  }
</style>
